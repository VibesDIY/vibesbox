{
  "name": "therawvibes",
  "version": "1.0.0",
  "description": "Cloudflare Worker for serving vibes at custom domains via iframe",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --port 8989",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging"
  },
  "keywords": ["cloudflare", "worker", "iframe", "vibes"],
  "author": "therawvibes",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.78.0"
  }
}